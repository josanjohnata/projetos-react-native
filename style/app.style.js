import { StyleSheet } from 'react-native';

export const appStyles = StyleSheet.create({
  header: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 20,
    color: 'red'
  },
  text: {
    textAlign: 'center',
    fontSize: 20,
    color: 'blue'
  }
});
